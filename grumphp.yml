grumphp:
  parameters:
    git_dir: .
    bin_dir: vendor/bin
  tasks:
    phpcs:
      standard: PSR12
      ignore_patterns: [
        ./tests/*,
        ./resources/*,
        ./database/*,
        ./bootstrap/*,
        ./public/*,
        ./config/*,
        ./routes/*
      ]